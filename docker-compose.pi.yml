# Extensiones de hardware para Raspberry Pi Zero W2.
# Combinar con: COMPOSE_FILE=docker-compose.yml:docker-compose.pi.yml
# Los montajes opcionales (USB bus, D-Bus) se generan en docker-compose.override.yml
# mediante scripts/install.sh según deploy.env.

services:
  nilocardmed:
    devices:
      - ${VIDEO_DEVICE_HOST:-/dev/video0}:${VIDEO_DEVICE_CONTAINER:-/dev/video0}
    group_add:
      - ${CONTAINER_GROUP_ADD:-video}
    # Pi Zero W2: limitar RAM para evitar OOM en el host (~512 MiB total)
    deploy:
      resources:
        limits:
          memory: ${CONTAINER_MEMORY_LIMIT:-384M}
    shm_size: ${CONTAINER_SHM_SIZE:-64m}
    environment:
      MALLOC_ARENA_MAX: ${MALLOC_ARENA_MAX:-2}
      PYTHONUNBUFFERED: "1"
